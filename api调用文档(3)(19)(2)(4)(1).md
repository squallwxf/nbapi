# KR API 下游接入文档

本文档用于下游客户接入 `https://ai.krapi.cn`。

## 1. 基础信息

接口地址：

```text
https://ai.krapi.cn
```

请求头：

```http
Authorization: Bearer sk-你的密钥
Content-Type: application/json
```

## 2. 模型列表

| 类型 | 模型名称 | 调用方式 |
| --- | --- | --- |
| Gemini 图片 | `T香蕉2` | Gemini 原生异步生图 |
| Gemini 图片 | `T香蕉pro` | Gemini 原生异步生图 |
| GPT 图片 | `gpt-image-2` | OpenAI 图片异步任务 |
| Grok 视频 | `grok-video-480p` | 视频异步任务 |
| Grok 视频 | `grok-video-720p` | 视频异步任务 |
| Grok 1.5 视频 | `grok-imagine-video-1.5-480p` | 视频异步任务，只支持单图 |
| Grok 1.5 视频 | `grok-imagine-video-1.5-720p` | 视频异步任务，只支持单图 |
| Omni 视频 | `omni-flash` | 视频异步任务 |
| Omni 视频 | `omni-flash-1080p` | 视频异步任务 |
| Omni 视频 | `omni-flash-4k` | 视频异步任务 |
| Omni 视频 | `omni-flash-components` | 视频异步任务 |
| Omni 视频 | `omni-flash-components-1080p` | 视频异步任务 |
| Omni 视频 | `omni-flash-components-4k` | 视频异步任务 |
| Omni 视频编辑 | `omni-flash-edit` | 视频异步任务 |
| Omni 视频编辑 | `omni-flash-edit-1080p` | 视频异步任务 |
| Omni 视频编辑 | `omni-flash-edit-4k` | 视频异步任务 |
| Sora 视频 | `sora-v3-fast` | 视频异步任务 |
| Sora 视频 | `sora-v3-fast-1080p` | 视频异步任务 |
| Sora 视频 | `sora-v3-pro` | 视频异步任务 |
| Sora 视频 | `sora-v3-pro-1080p` | 视频异步任务 |
| Veo 视频 | `veo-3.1-lite-720` | 视频异步任务 |
| Veo 视频 | `veo-3.1-lite-1080` | 视频异步任务 |
| Veo 视频 | `veo-3.1-lite-4k` | 视频异步任务 |
| Veo 视频 | `veo-3.1-fast-720` | 视频异步任务 |
| Veo 视频 | `veo-3.1-fast-1080` | 视频异步任务 |
| Veo 视频 | `veo-3.1-fast-4k` | 视频异步任务 |
| Veo 视频 | `veo-3.1-quality-720` | 视频异步任务 |
| Veo 视频 | `veo-3.1-quality-1080` | 视频异步任务 |
| Veo 视频 | `veo-3.1-quality-4k` | 视频异步任务 |

## 3. Gemini 图片生成

Gemini 图片使用原生异步接口。

请求地址：

```http
POST https://ai.krapi.cn/v1beta/models/{model}:generateContent
```

可用模型：

```text
T香蕉2
T香蕉pro
```

实际代码里建议对 `{model}` 做 URL 编码：

```js
const model = "T香蕉2";
const url = `https://ai.krapi.cn/v1beta/models/${encodeURIComponent(model)}:generateContent`;
```

### 3.1 文生图

```bash
curl -X POST "https://ai.krapi.cn/v1beta/models/T香蕉2:generateContent" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [
      {
        "role": "user",
        "parts": [
          {
            "text": "生成一张写实摄影风格的橙色跑车，城市夜景，电影感，高质量"
          }
        ]
      }
    ],
    "generationConfig": {
      "responseModalities": ["TEXT", "IMAGE"],
      "imageConfig": {
        "aspectRatio": "16:9",
        "imageSize": "2K"
      }
    }
  }'
```

提交成功会返回 `task_id`，再通过图片任务查询接口获取结果。

### 3.2 图生图 / 多图参考

图片使用 `inlineData`，`data` 填 base64 图片内容，不要带 `data:image/jpeg;base64,` 前缀。

图片顺序按 `parts` 里的顺序读取。多图参考时，建议使用“文字说明 + 图片”的顺序。

```bash
curl -X POST "https://ai.krapi.cn/v1beta/models/T香蕉2:generateContent" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "contents": [
      {
        "role": "user",
        "parts": [
          {
            "text": "图片1：人物主体参考图。"
          },
          {
            "inlineData": {
              "mimeType": "image/jpeg",
              "data": "这里填写第1张图片base64"
            }
          },
          {
            "text": "图片2：服装参考图。"
          },
          {
            "inlineData": {
              "mimeType": "image/jpeg",
              "data": "这里填写第2张图片base64"
            }
          },
          {
            "text": "请保留图片1的人物身份和姿势，换成图片2的服装，写实摄影风格。"
          }
        ]
      }
    ],
    "generationConfig": {
      "responseModalities": ["TEXT", "IMAGE"],
      "imageConfig": {
        "aspectRatio": "3:4",
        "imageSize": "2K"
      }
    }
  }'
```

### 3.3 图片参数

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `contents` | 是 | Gemini 原生内容结构 |
| `parts[].text` | 是 | 提示词或图片说明 |
| `parts[].inlineData` | 否 | 参考图 base64 |
| `generationConfig.responseModalities` | 是 | 建议固定传 `["TEXT", "IMAGE"]` |
| `generationConfig.imageConfig.aspectRatio` | 否 | 图片比例，例如 `1:1`、`3:4`、`4:3`、`9:16`、`16:9` |
| `generationConfig.imageConfig.imageSize` | 否 | 图片规格，例如 `1K`、`2K`、`4K` |

### 3.4 图片任务查询

请求地址：

```http
GET https://ai.krapi.cn/v1/images/tasks/{task_id}
```

查询示例：

```bash
curl "https://ai.krapi.cn/v1/images/tasks/task_xxx" \
  -H "Authorization: Bearer sk-你的密钥"
```

完成返回示例：

```json
{
  "id": "task_xxx",
  "task_id": "task_xxx",
  "object": "image.task",
  "status": "succeeded",
  "progress": 100,
  "created_at": 1780000000,
  "completed_at": 1780000048,
  "model": "T香蕉2",
  "data": [
    {
      "url": "https://example.com/result.jpg"
    }
  ]
}
```

图片地址优先读取：

```text
data[0].url
```

如果没有 `data[0].url`，再读取：

```text
result.data[0].url
```

## 4. GPT 图片生成

`gpt-image-2` 使用 OpenAI 图片接口，提交成功后返回 `task_id`，再通过图片任务查询接口获取结果。

可用模型：

```text
gpt-image-2
```

### 4.1 文生图

请求地址：

```http
POST https://ai.krapi.cn/v1/images/generations
```

```bash
curl -X POST "https://ai.krapi.cn/v1/images/generations" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-image-2",
    "prompt": "一只白色小猫坐在窗边，电影感，真实摄影风格",
    "n": 1,
    "size": "1024x1024",
    "quality": "auto",
    "response_format": "url"
  }'
```

### 4.2 图生图：上传本地图片

请求地址：

```http
POST https://ai.krapi.cn/v1/images/edits
```

本地图片使用 `multipart/form-data`：

```text
单张图片字段：image
多张图片字段：image[]
```

PowerShell 示例：

```powershell
$apiKey = "sk-你的密钥"

$form = @{
  model = "gpt-image-2"
  prompt = "参考图片里的主体，换成赛博朋克城市背景，保持主体清晰"
  n = "1"
  size = "1024x1024"
  quality = "auto"
  response_format = "url"
  image = Get-Item "C:\Users\你的用户名\Desktop\ref.jpg"
}

Invoke-RestMethod `
  -Uri "https://ai.krapi.cn/v1/images/edits" `
  -Method Post `
  -Headers @{ Authorization = "Bearer $apiKey" } `
  -Form $form
```

### 4.3 图生图：上传图片 URL

如果图片已经是公网 URL，可以直接传 URL。

```text
单张 URL 字段：image_url
多张 URL 字段：image_url[]
```

PowerShell 示例：

```powershell
$apiKey = "sk-你的密钥"

$form = @{
  model = "gpt-image-2"
  prompt = "参考图片里的主体，生成一张写实风格商业海报"
  n = "1"
  size = "1024x1024"
  quality = "auto"
  response_format = "url"
  image_url = "https://example.com/ref.jpg"
}

Invoke-RestMethod `
  -Uri "https://ai.krapi.cn/v1/images/edits" `
  -Method Post `
  -Headers @{ Authorization = "Bearer $apiKey" } `
  -Form $form
```

### 4.4 GPT 图片参数

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `model` | 是 | 固定传 `gpt-image-2` |
| `prompt` | 是 | 图片提示词 |
| `n` | 否 | 生成张数，默认 `1` |
| `size` | 否 | 官方尺寸或比例，例如 `1024x1024`、`1536x1024`、`1024x1536`、`16:9`、`9:16`、`1:1` |
| `image_size` | 否 | 比例写法时可传 `1K`、`2K`、`4K`，不传默认按 `2K` 处理 |
| `quality` | 否 | 支持 `auto`、`high`、`medium`、`low`，建议 `auto` |
| `response_format` | 否 | 建议传 `url` |
| `image` / `image[]` | 否 | 本地图片上传字段，用于图生图 |
| `image_url` / `image_url[]` | 否 | 图片 URL 字段，用于图生图 |

提交成功后，查询方式和 Gemini 图片一致：

```http
GET https://ai.krapi.cn/v1/images/tasks/{task_id}
```

图片地址常见位置：

```text
result.data[0].url
data[0].url
url
```

## 5. 视频图片参数通用规则

视频接口里的参考图统一使用 `images` 数组。单图也传数组，不要传 `image` 字段。

正确：

```json
{
  "images": ["https://example.com/input.jpg"]
}
```

错误：

```json
{
  "image": ["https://example.com/input.jpg"]
}
```

## 6. Grok 视频生成

Grok 视频使用视频异步任务接口。

请求地址：

```http
POST https://ai.krapi.cn/v1/videos
```

可用模型：

```text
grok-video-480p
grok-video-720p
grok-imagine-video-1.5-480p
grok-imagine-video-1.5-720p
```

### 6.1 Grok Video 1.0 文生视频

```bash
curl -X POST "https://ai.krapi.cn/v1/videos" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "grok-video-720p",
    "prompt": "a futuristic city street, handheld camera, neon lights",
    "seconds": "16",
    "aspect_ratio": "16:9",
    "n": 1
  }'
```

### 6.2 Grok Imagine Video 1.5 图生视频

Grok Imagine Video 1.5 必须传 `images`，并且只支持 1 张参考图。

```bash
curl -X POST "https://ai.krapi.cn/v1/videos" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "grok-imagine-video-1.5-720p",
    "prompt": "make the person walk forward and smile",
    "seconds": "10",
    "images": [
      "https://example.com/input.jpg"
    ],
    "aspect_ratio": "9:16",
    "n": 1
  }'
```

### 6.3 Grok 参数

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `model` | 是 | 使用上面的 Grok 模型名称 |
| `prompt` | 是 | 视频提示词 |
| `seconds` | 是 | 必须传字符串。`grok-video-*` 支持 `"10"`、`"16"`、`"20"`；`grok-imagine-video-1.5-*` 支持 `"6"`、`"10"`、`"12"`、`"15"` |
| `images` | 按模型 | 图片 URL 数组。`grok-imagine-video-1.5-*` 必须且只能传 1 张图 |
| `aspect_ratio` | 否 | 支持 `1:1`、`16:9`、`9:16`、`4:3`、`3:4`、`3:2`、`2:3` |
| `n` | 否 | 生成条数，通常传 `1` |

说明：

- `seconds` 请传字符串，例如 `"10"`。
- `grok-imagine-video-1.5-*` 只支持单图，`images` 里只能放 1 个图片 URL。
- 不要传 `image` 字段，图片统一放在 `images` 数组里。

## 7. Omni 视频生成 / 编辑

Omni 使用视频异步任务接口。

请求地址：

```http
POST https://ai.krapi.cn/v1/videos
```

可用模型：

```text
omni-flash
omni-flash-1080p
omni-flash-4k
omni-flash-components
omni-flash-components-1080p
omni-flash-components-4k
omni-flash-edit
omni-flash-edit-1080p
omni-flash-edit-4k
```

### 7.1 Omni Flash 文生视频

Omni Flash 需要传 `seconds`，必须是字符串，支持 `"4"`、`"6"`、`"8"`、`"10"`。

```bash
curl -X POST "https://ai.krapi.cn/v1/videos" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "omni-flash-4k",
    "prompt": "sunset over ocean, cinematic, smooth camera movement",
    "seconds": "8",
    "aspect_ratio": "16:9",
    "n": 1
  }'
```

### 7.2 Omni Components 参考图生视频

Omni Components 需要传 `seconds` 和 `images`。

```bash
curl -X POST "https://ai.krapi.cn/v1/videos" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "omni-flash-components-1080p",
    "prompt": "make the subject walk forward naturally",
    "seconds": "8",
    "images": [
      "https://example.com/input.jpg"
    ],
    "aspect_ratio": "9:16",
    "n": 1
  }'
```

### 7.3 Omni Edit 视频编辑

Omni Edit 不需要传 `seconds`，但需要传 `video_url`。

```bash
curl -X POST "https://ai.krapi.cn/v1/videos" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "omni-flash-edit-1080p",
    "prompt": "add lightning effect, cinematic lighting",
    "video_url": "https://example.com/ref-video.mp4",
    "aspect_ratio": "16:9",
    "n": 1
  }'
```

### 7.4 Omni 参数

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `model` | 是 | 使用上面的 Omni 模型名称 |
| `prompt` | 是 | 视频提示词 |
| `seconds` | 按模型 | `omni-flash*` 和 `omni-flash-components*` 需要，支持 `"4"`、`"6"`、`"8"`、`"10"` |
| `images` | 按模型 | `omni-flash-components*` 需要图片 URL 数组 |
| `video_url` | 按模型 | `omni-flash-edit*` 需要视频 URL |
| `aspect_ratio` | 否 | 支持 `16:9` 或 `9:16` |
| `n` | 否 | 生成条数，通常传 `1` |

## 8. Sora-v3 视频生成

Sora-v3 使用视频异步任务接口。

请求地址：

```http
POST https://ai.krapi.cn/v1/video/submit/generate
```

可用模型：

```text
sora-v3-fast
sora-v3-fast-1080p
sora-v3-pro
sora-v3-pro-1080p
```

### 8.1 文生视频

不传 `images` 时，按文生视频处理。

```bash
curl -X POST "https://ai.krapi.cn/v1/video/submit/generate" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-v3-fast",
    "prompt": "一辆黑色跑车在雨夜城市街道缓慢驶过，电影感，镜头平稳，高质量",
    "duration": 8,
    "metadata": {
      "modeType": "text2video",
      "ratio": "16:9",
      "enableSound": "on"
    }
  }'
```

### 8.2 图生视频

传 1 张图片时，按图生视频处理。

```bash
curl -X POST "https://ai.krapi.cn/v1/video/submit/generate" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-v3-fast",
    "prompt": "让画面中的人物缓缓转头微笑，动作自然，画面稳定",
    "duration": 8,
    "images": [
      "https://example.com/input.jpg"
    ],
    "metadata": {
      "modeType": "image2video",
      "ratio": "16:9",
      "enableSound": "on"
    }
  }'
```

### 8.3 首尾帧视频

传 2 张图片时，按首尾帧视频处理。`images[0]` 是首帧，`images[1]` 是尾帧。

```bash
curl -X POST "https://ai.krapi.cn/v1/video/submit/generate" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "sora-v3-fast-1080p",
    "prompt": "从第一张图自然过渡到第二张图，动作流畅，画面稳定",
    "duration": 8,
    "images": [
      "https://example.com/start.jpg",
      "https://example.com/end.jpg"
    ],
    "metadata": {
      "modeType": "frames2video",
      "ratio": "16:9",
      "enableSound": "on"
    }
  }'
```

### 8.4 Sora 参数

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `model` | 是 | 使用上面的 Sora-v3 模型名称 |
| `prompt` | 是 | 视频提示词 |
| `duration` | 是 | 视频时长，单位秒，例如 `8` |
| `images` | 否 | 图片 URL 数组。不传为文生视频，1 张为图生视频，2 张为首尾帧视频 |
| `metadata.modeType` | 是 | `text2video`、`image2video` 或 `frames2video` |
| `metadata.ratio` | 否 | 画面比例，例如 `16:9`、`9:16`、`1:1` |
| `metadata.enableSound` | 否 | 是否生成音频，`on` 或 `off` |

## 9. Veo 视频生成

Veo 使用视频异步任务接口。

请求地址：

```http
POST https://ai.krapi.cn/v1/videos
```

可用模型：

```text
veo-3.1-lite-720
veo-3.1-lite-1080
veo-3.1-lite-4k
veo-3.1-fast-720
veo-3.1-fast-1080
veo-3.1-fast-4k
veo-3.1-quality-720
veo-3.1-quality-1080
veo-3.1-quality-4k
```

Veo 按次计费，不需要传 `seconds`。

### 9.1 文生视频

```bash
curl -X POST "https://ai.krapi.cn/v1/videos" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "veo-3.1-fast-1080",
    "prompt": "orange cat chasing butterfly, cinematic camera motion",
    "aspect_ratio": "16:9",
    "n": 1
  }'
```

### 9.2 图生视频

```bash
curl -X POST "https://ai.krapi.cn/v1/videos" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "veo-3.1-fast-1080",
    "prompt": "make cinematic, smooth camera motion",
    "images": [
      "https://example.com/input.jpg"
    ],
    "aspect_ratio": "16:9",
    "n": 1
  }'
```

### 9.3 首尾帧视频

`images` 传 2 张图时，第 1 张是首帧，第 2 张是尾帧。

```bash
curl -X POST "https://ai.krapi.cn/v1/videos" \
  -H "Authorization: Bearer sk-你的密钥" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "veo-3.1-fast-4k",
    "prompt": "从第一张图自然过渡到第二张图，动作流畅，画面稳定，高质量",
    "images": [
      "https://example.com/start.jpg",
      "https://example.com/end.jpg"
    ],
    "aspect_ratio": "9:16",
    "n": 1
  }'
```

### 9.4 Veo 参数

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| `model` | 是 | 使用上面的 Veo 模型名称 |
| `prompt` | 是 | 视频提示词 |
| `images` | 否 | 图片 URL 数组。不传为文生视频，1 张为图生视频，2 张为首尾帧视频 |
| `aspect_ratio` | 否 | 支持 `16:9` 或 `9:16`，默认 `16:9` |
| `n` | 否 | 生成条数，通常传 `1` |

说明：

- 图片统一放在 `images` 数组里，单图也写成 `images: ["https://xxx.jpg"]`。
- 不要传 `image` 字段。
- 不要传 `seconds`。

## 10. 视频任务返回和查询

### 10.1 Grok / Omni / Veo 查询

Grok、Omni 和 Veo 使用同一个查询接口：

```http
GET https://ai.krapi.cn/v1/videos/{task_id}
```

查询示例：

```bash
curl "https://ai.krapi.cn/v1/videos/task_xxx" \
  -H "Authorization: Bearer sk-你的密钥"
```

### 10.2 Sora 查询

Sora-v3 使用下面的查询接口：

```http
GET https://ai.krapi.cn/v1/video/fetch/{task_id}
```

查询示例：

```bash
curl "https://ai.krapi.cn/v1/video/fetch/task_xxx" \
  -H "Authorization: Bearer sk-你的密钥"
```

### 10.3 提交成功返回示例

```json
{
  "id": "task_xxx",
  "task_id": "task_xxx",
  "object": "video",
  "model": "sora-v3-fast",
  "status": "queued",
  "progress": 0,
  "created_at": 1780000000
}
```

请保存 `task_id`，用于后续轮询。

### 10.4 生成中返回示例

```json
{
  "id": "task_xxx",
  "task_id": "task_xxx",
  "object": "video",
  "status": "processing",
  "progress": 50,
  "created_at": 1780000000
}
```

### 10.5 生成成功返回示例

```json
{
  "id": "task_xxx",
  "task_id": "task_xxx",
  "object": "video",
  "status": "completed",
  "progress": 100,
  "created_at": 1780000000,
  "completed_at": 1780000080,
  "url": "https://ai.krapi.cn/file/xxx",
  "video_url": "https://ai.krapi.cn/file/xxx",
  "metadata": {
    "url": "https://ai.krapi.cn/file/xxx"
  },
  "data": [
    {
      "url": "https://ai.krapi.cn/file/xxx"
    }
  ],
  "result": {
    "file_url": "https://ai.krapi.cn/file/xxx",
    "file_ext": "mp4"
  }
}
```

视频地址建议优先读取：

```text
url
```

如果没有 `url`，再按顺序读取：

```text
video_url
metadata.url
data[0].url
result.file_url
```

网页里可以直接使用：

```html
<video src="返回的视频url" controls></video>
```

### 10.6 失败返回示例

```json
{
  "id": "task_xxx",
  "task_id": "task_xxx",
  "object": "video",
  "status": "failed",
  "progress": 100,
  "error": {
    "message": "失败原因"
  }
}
```

## 11. 任务状态和轮询建议

图片任务状态：

| 状态 | 说明 |
| --- | --- |
| `queued` | 已提交，等待执行 |
| `processing` | 正在生成 |
| `succeeded` | 生成成功 |
| `failed` | 生成失败 |

视频任务状态：

| 状态 | 说明 |
| --- | --- |
| `queued` | 已提交，等待执行 |
| `processing` | 正在生成 |
| `completed` | 生成成功 |
| `failed` | 生成失败 |

建议轮询间隔：

| 类型 | 间隔 |
| --- | --- |
| 图片 | 每 2 到 5 秒查询一次 |
| 视频 | 每 5 到 10 秒查询一次 |

轮询到 `succeeded`、`completed` 或 `failed` 后停止。

## 12. JavaScript 轮询示例

```js
async function pollImageTask(apiKey, taskId) {
  for (let i = 0; i < 180; i += 1) {
    await new Promise((resolve) => setTimeout(resolve, 3000));

    const res = await fetch(`https://ai.krapi.cn/v1/images/tasks/${encodeURIComponent(taskId)}`, {
      headers: {
        Authorization: `Bearer ${apiKey}`,
      },
    });
    const task = await res.json();

    if (task.status === "succeeded") {
      return task.data?.[0]?.url || task.result?.data?.[0]?.url;
    }
    if (task.status === "failed") {
      throw new Error(task.error?.message || "image task failed");
    }
  }
  throw new Error("image task timeout");
}

async function pollVideoTask(apiKey, taskId, type = "standard") {
  const queryUrl =
    type === "sora"
      ? `https://ai.krapi.cn/v1/video/fetch/${encodeURIComponent(taskId)}`
      : `https://ai.krapi.cn/v1/videos/${encodeURIComponent(taskId)}`;

  for (let i = 0; i < 180; i += 1) {
    await new Promise((resolve) => setTimeout(resolve, 5000));

    const res = await fetch(queryUrl, {
      headers: {
        Authorization: `Bearer ${apiKey}`,
      },
    });
    const task = await res.json();

    if (task.status === "completed") {
      return task.url || task.video_url || task.metadata?.url || task.data?.[0]?.url || task.result?.file_url;
    }
    if (task.status === "failed") {
      throw new Error(task.error?.message || "video task failed");
    }
  }
  throw new Error("video task timeout");
}
```
