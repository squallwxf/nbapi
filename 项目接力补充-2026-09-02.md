# NBAPI 项目接力补充 2026-09-02

这是对 `项目接力说明.md` 的补充。当前原文档可能被 Word/WPS 占用，如果要合并进去，先关闭正在打开的文档。

## 本次已确认

- 注册功能已经接到真实后端，用户会写入 `nbapi.sqlite3`
- 注册成功后会自动创建会话并登录
- 新注册用户默认角色为 `user`，默认启用，默认额度为 `100.000000`
- 登录接口已经重新验证通过，`/api/auth/login` 能返回 session token 和用户角色
- 管理员接口 `/api/admin/users` 已能读取真实用户列表
- 令牌创建会绑定当前登录用户，新 Key 只在创建成功响应中返回一次
- 令牌列表只显示脱敏值，服务端使用哈希鉴权，不保存可复制的明文 Key
- 旧版本令牌的哈希仍可正常鉴权，但遗失完整 Key 后不能从网站恢复
- 当前浏览器会缓存新建令牌的完整值，列表可以直接复制；更换设备或清理浏览器数据后无法恢复遗失的 Key
- 令牌创建已补全分组、过期时间、批量数量、额度/无限额度、模型白名单和 IP/CIDR 白名单，并在真实模型调用时生效
- 令牌管理已补充单个/批量启用、禁用和删除功能，删除令牌不会删除历史扣费记录
- 新增操练场页面，支持完整模型列表；请求按模型类型进入对应代理、鉴权与计费链路
- 已补充图片任务轮询和预览，查询接口为 `/v1/images/tasks/{task_id}`
- 已补充调用后的服务器余额刷新和实际扣费金额显示，用于核对上游调用与 NBAPI 账单是否联动

## 当前权限逻辑

- 未登录：显示“登录”和“注册”
- 已登录：隐藏“登录”和“注册”
- 普通用户：可进入控制台、模型广场、令牌、文档等用户功能
- 管理员和超级管理员：额外显示“用户管理”和“渠道管理”
- 管理员：可把普通用户提升为管理员、调整额度、启停用户、重置密码
- 超级管理员：在管理员能力基础上，还可设置超级管理员角色

## 本地到服务器的同步方式

- 本地完成的代码可以上传到服务器使用，不需要在服务器重新做一遍功能
- 只改 `api-website.html`：上传前端文件后刷新页面即可
- 改 `server.py`：上传后需要重启后端服务

## 2026-09-06 供应商与模型管理补充

- 最新提交为 `b183214`，已上传到 `squallwxf/nbapi` 的 `main` 分支。
- 超级管理员控制台新增“供应商对接”，可设置上游根地址、API Key、优先级、备注以及渠道启停。普通管理员和普通用户无权访问。
- 服务器默认上游地址可填写 `https://ai.krapi.cn`；如果填写带 `/v1` 或 `/v1beta` 的地址，后端会自动去掉版本后缀。
- 模型定价页面新增“已可见/已隐藏”状态，以及隐藏、显示、永久删除操作。隐藏是可恢复的，删除不可恢复。
- 普通模型列表只返回已启用模型，超级管理员定价页会额外读取隐藏模型。
- 当前新增模型 `gpt-6-astra`，调用端点为 `/v1/chat/completions`；价格暂按 `gpt-5.5` 初始化，需确认后再运营。
- Krapi 模型广场没有 GROK；Sora 模型需要使用上游 API Key 实际拥有的渠道和分组权限，模型存在不等于该 Key 一定可调用。

## 服务器更新命令

```bash
cd /opt/nbapi
git pull --ff-only origin main
systemctl daemon-reload
systemctl restart nbapi
systemctl is-active nbapi
```
- 改 `nbapi.nginx`：上传后需要重新加载 Nginx
- 改数据库结构、默认账号或已有数据：需要谨慎处理服务器上的 `nbapi.sqlite3`，先备份再迁移
- 不建议把本地 `nbapi.sqlite3` 直接覆盖服务器数据库，除非明确要覆盖线上用户、令牌、余额和渠道密钥

## 服务器上线要点

- 代码目录：`/opt/nbapi`
- 线上域名：`https://nbapi.win`
- 后端服务名：`nbapi`
- 更新后端后执行：`sudo systemctl restart nbapi`
- 更新 Nginx 后执行：`sudo nginx -t && sudo systemctl reload nginx`
- 线上上游 API Key 建议在网站后台填写，不写进 Git 仓库

## 后续优先级

1. 补全各模型的接入示例和文档区内容
2. 补上真实上游代理转发逻辑
3. 把控制台日志接到真实 ledger / 请求日志数据
4. 完善渠道健康检测、失败重试和优先级路由
5. 继续细化用户管理的搜索、分页、角色调整和操作确认
