# NBAPI 项目接力说明

这是当前仓库的接力笔记，目的是让换电脑、换会话、换时间继续开发时，能快速接上。

## 当前定位

NBAPI 是一个面向 AI 模型聚合分发的站点，目标是对齐 `https://ai.krapi.cn` 的控制台、模型广场和接入方式。

## 关键文件

- `api-website.html`：前端单页，控制台、模型广场、登录注册、系统设置都在这里
- `server.py`：本地/线上后端，负责登录、令牌、计费、模型价格、上游配置
- `api调用文档(3)(19)(2)(4)(1).md`：上游调用文档
- `nbapi.nginx`：线上 Nginx 反代示例
- `nbapi.service`：线上 systemd 服务示例

## 当前已实现

- 登录 / 退出
- 三层用户等级：超级管理员、管理员、用户
- 令牌创建、禁用、列表、复制完整 API Key
- 模型广场筛选、搜索、价格切换
- 控制台总览、用户管理、渠道管理、日志、系统设置
- 管理员上游 API Key 配置
- 用户管理：管理员可调整用户剩余额度、提升普通用户为管理员；超级管理员还可设置超级管理员角色
- 渠道管理：管理员可新增和编辑渠道名称、上游地址、上游 API Key、优先级、启停状态和备注

## 接入规则

- GPT 系列：按 OpenAI 官方兼容方式调用
- 其他模型：按文档里的原生接口调用
- 统一站点基址：`https://ai.krapi.cn`
- 下游调用鉴权：使用站点内创建的 `nb_sk_...` 令牌；令牌会自动绑定创建它的登录用户
- 上游配置：在“系统设置”里填写并保存，后端落到 `settings` 表
- 渠道配置：在“渠道管理”里维护，后端落到 `channels` 表

## 本地启动

1. 启动后端：

```bash
python server.py
```

2. 启动静态预览：

```bash
python -m http.server 8000 --bind 127.0.0.1
```

3. 打开：

```text
http://127.0.0.1:8000/api-website.html
```

后端默认监听 `127.0.0.1:8765`。

## 本地上传服务器

以后建议通过 GitHub 同步代码，避免手动逐个复制文件。

本地修改完成后，在 PowerShell 中执行：

```powershell
cd "D:\\ai web"
git add .
git commit -m "更新网站"
git push
```

服务器 SSH 登录后执行：

```bash
cd /opt/nbapi
git pull
```

修改 `server.py` 后重启服务：

```bash
systemctl restart nbapi
```

修改 `nbapi.nginx` 后检查并重新加载：

```bash
nginx -t && systemctl reload nginx
```

只修改 `api-website.html` 时，拉取后刷新浏览器即可，必要时按 `Ctrl+F5`。

不要用本地数据库覆盖线上 `nbapi.sqlite3`。线上数据库包含用户、余额、API Key、扣费记录和渠道密钥；如需数据库变更，应先备份并单独迁移。上游 API Key、服务器密码等敏感信息也不要提交到 GitHub。

## 默认账号

- 超级管理员：`squallwxf` / `Aa19860120`
- 管理员：`admin` / `admin123`
- 普通用户：`demo` / `demo123`

## 数据位置

- `nbapi.sqlite3`：用户、令牌、余额、计费、模型价格、上游配置、渠道配置
- `nbapi.sqlite3-wal` / `nbapi.sqlite3-shm`：运行时文件，不要提交

## 令牌规则（2026-09-02）

- 新建令牌时完整 Key 只返回一次，页面提示用户立即保存。
- 令牌列表不再返回完整 Key，只返回脱敏值。
- 服务端使用 SHA-256 哈希进行鉴权，不再保存可复制的明文 Key。
- 停用令牌或停用所属用户后，调用会立即被拒绝。
- 旧版本创建的令牌仍可正常鉴权，但完整 Key 无法从网站恢复；遗失后需要重新创建。
- 当前浏览器会按用户名缓存新建令牌的完整值，列表提供“复制 Key”按钮；更换设备或清理浏览器数据后无法恢复遗失的 Key。
- 创建令牌支持分组、过期时间、批量数量、额度/无限额度、模型白名单和 IP/CIDR 白名单；这些参数会关联到真实调用鉴权与计费。
- 令牌支持单个/批量启用、禁用和删除；删除只移除令牌，不删除历史扣费记录。
- 新增操练场页面，可选择当前浏览器中的用户令牌和完整模型列表；系统按模型类型调用对话、Gemini、图片或视频端点，调用会按模型价格扣除余额。
- 图片任务会自动轮询任务状态，成功后显示生成图片；图片提交只扣一次费用，查询任务不会重复扣费。

## 这次接力最重要的点

1. 不是只做外观，前后端都已经有状态联动
2. 登录态会按后端角色判断权限，登录后页面会隐藏“登录”和“注册”入口
3. 上游 API Key 可以在系统设置页保存，也可以在渠道管理中按渠道保存
4. 模型广场的模型名、接口、计费方式已经和文档对齐了大半

## 继续开发建议

1. 把各模型的接入示例再补全到前端文档区
2. 把 `server.py` 的上游代理转发补上
3. 再把控制台日志和渠道健康数据接成真实数据源
